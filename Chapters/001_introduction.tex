\chapter{Introduction}
\label{Introduction}

Nowadays, peer-to-peer (P2P) networks, which enable decentralized file-sharing, resource sharing, and content distribution without depending on centralized servers, have greatly influenced the current state of the internet. It is used in distributed file systems, blockchain technology, online gaming, and video streaming platforms. A significant portion of the world's internet traffic is powered by P2P architectures. As a result, it offers increased resistance to single points of failure, better fault tolerance, lower infrastructure costs, and improved scalability.

Among P2P protocols, BitTorrent stands as one of the most successful and widely deployed implementations. It's efficient by fragmenting files into smaller chunks that can be individually uploaded to the swarm rather than a single source exposing all chunks, like regular HTTP transfers. This architecture has been proven remarkably effective for distributing large files, from open-source software and academic datasets to media content, having a heavy usage on the piracy world.
%% ai like %%

However, despite its adoption and efficiency, BitTorrent and other similar P2P protocols suffer from critical security and privacy vulnerabilities, that can jeopardize users safety and information. Many of which, can cause exposure to malicious peers who may distribute harmful content disguised as legitimate files. It is susceptible to traffic analysis, exposing IP addresses making participants vulnerable to target de-anonymization attacks, subjected to surveillance, no privacy. Furthermore, the ecosystem relies on unverified client implementations that could contain malicious code or vulnerabilities and other security issues, but mainly peers can't authenticate each other, thus allowing to be targeted by malicious peers. Despite, the distributed nature of the network of providing resilience, fault tolerance, at the same time may put in danger the peer identities. It relies heavily on an environment of trust that is difficult to establish, where malicious actors can operate with relative impunity, discrediting the overall public network.

There are some solutions that may mitigate these issues stated earlier, as the usage of protocol encryption (MSE/PE) or VPN tunnelling, that offer partial protection. Although, encryption primarily address ISP throttling rather than peer authentication or real anonymity. Usually, VPNs shift trust to third-party providers, most of the time keeping record of their activities. Also, they are prone in cases where they are used without the user's knowledge to de-anonymize them. Not only that, but they often impact performance significantly. On the other side, there are anonymity networks, like Tor explicitly discourage BitTorrent traffic due to the high bandwidth usage. Most concerning, there isn't any real methodology to tackle the core problem with peer trustworthiness, as there aren't any viable mechanism to verify that connecting peers are compromised and if they can jeopardize others peers on the network.

This thesis explores a novel approach addressing these stated security and privacy challenges in P2P file-sharing through the integration of three complementary technologies: \textbf{Trusted Execution Environment (TEE)} based client attestation, the \textbf{QUIC} transport protocol, and onion routing techniques. Combining secure hardware, modern network transport security, and traffic anonymization, this research investigates whether it is possible to achieve better security without compromising overall performance.

\section{Motivation}
This thesis proposes a proof-of-concept BitTorrent Protocol that tries to mitigate these challanges by combining the following three integrated mechanisms:

\begin{itemize}
  \item \textbf{TEE based Client Attestation}: Taking advantage of Trusted Execution Environments solutions provided by hardware platforms such as \textbf{Intel SGX/TDX}, \textbf{ARM TrustZone}, or \textbf{AMD SEV-SNP}, this mechanism enables peers to cryptographically attest to the authenticity and integrity of their client implementations. For example, when a peer joins the swarm, it can prove to the other participants that it is running a known, unmodified client implementation within a secure enclave. This approach may prevent peers from accepting connections from compromised or malicious clients, thereby reducing significantly the attack surface against content poisoning, protocol manipulation, and other attacks related to P2P. This approach differentiates depends on hardware specific features that require on trusting third-party certificates authorities or centralized identity providers, and can improve overall security.

  \item \textbf{Quic}: By integrating the QUIC protocol (Quick UDP Internet Connections), a standard for the foundation for the HTTP/3, can provide many advantages over BitTorrent's current TCP based transport. As an example, QUIC offers mandatory encryption by default, reducing passive eavesdropping opportunities. Its multiplexed stream architecture eliminates head-of-line blocking, potentially improving download efficiency when receiving chunks from several peers in parallel. This thesis will also focus on ways to implement QUIC as the native transport layer for both peer discovery mechanisms (DHT and PEX) and chunk exchange inside the swarm, improving performance, security, and implementation complexity implications.

  \item \textbf{Anonymity techniques}: To address traffic analysis and IP exposure vulnerabilities, this research evaluates how to integrate onion routing techniques like \textbf{Tor}, \textbf{I2P}, and \textbf{VPNs}. Rather than directly connecting to peers and exposing IP address to all swarm participants, in order to protect privacy and anonymity, ideally routing the connections through multiple relays, with each hop knowing only the next node. While, trying to solve the problem of the last node, that can be susceptible to traffic analysis. This approach aims to prevent:

  \begin{itemize}
    \item \textbf{Traffic Analysis}: Adversaries attempt to correlate network traffic patterns between entry and exit nodes to infer user behaviour and de-anonymize their activities within the network.

    \item \textbf{IP Leakage}: Malicious entry nodes or network intermediaries may log connection metadata. For instance, compromised Tor entry nodes can record client IP addresses, while BitTorrent trackers maintain logs of peer addresses requesting to join swarms.

    \item \textbf{De-Anonymization Attacks}: Adversaries correlate user activity across multiple networks or sessions through traffic fingerprinting and timing analysis, enabling identification of users despite anonymization efforts.
  \end{itemize}

  Also, the research investigates the feasibility of integrating onion routing into the BitTorrent protocol while keeping in mind critical performance trade-offs. Specifically, analysing the bandwidth overhead introduced by relay nodes, latency increases by the multi-hop routing, mainly the dilemma of anonymity guarantees and practical usability for end users.

\end{itemize}

The main challenge addressed by this thesis is inherent tension between security, anonymity, and performance in P2P systems, while trying to keep it more secure. By introducing each protective measure, it will bring more computational and network overhead. Tee attestation requires cryptographic operations and trusted hardware availability. Also, adding QUIC encryption will add more processing cost. Onion routing will certainly increase the consumption of bandwidth and latency.

This study aims to evaluate how these technologies interact when integrated into BitTorrent protocol, proposing a proof-of-concept secure file-sharing protocol that attempts to mitigate the vulnerabilities described earlier. For instance, the research will tackle the performance issues and the balance between security, anonymity, efficiency, and perhaps its usability.

\begin{quote}
  \textit{Can TEE-based attestation combined with QUIC and onion routing significantly improve security and anonymity in P2P file-sharing without unacceptable performance penalties?}
\end{quote}

\section{Objectives}
\label{Objectives}
  To investigate further the research question, this thesis sets this research objectives:

  \begin{itemize}
    \item \textbf{Design and integrate TEE based peer attestation}: Develop a remote attestation protocol that allows the BitTorrent peers to verify each other's client implementation integrity and authenticity, using the TEE capabilities.

    \item \textbf{Integrate QUIC as the native transport protocol}: Replace the tradition TCP and UDP based communication with QUIC for the chunk exchange and peer discovery mechanisms, while measuring the impact on connection time, throughput, and performance.

    \item \textbf{Explore onion routing integration}: Research possible ways to integrate BitTorrent traffic through layered encryption circuits, and analysing the bandwidth multiplication factor, latency penalties.

    \item \textbf{Evaluate performance trade-offs}: Capture metrics comparing the proof-of-concept against the standard BitTorrent implementations, quantifying their performance penalties, and taking into consideration kpis like: download speed, resource consumption, and latency.
  \end{itemize}
